<% semantic_form_for([:admin, parent.restaurant, parent, resource]) do |f| %>
  <% f.inputs do %>
    <%= f.input :order %>
    <%= f.input :product %>
    <%= f.input :quantity %>
    <%= f.input :discount, :as => :select, :collection => discounts, :prompt => false %>
  <% end %>
  <%= f.buttons %>
<% end %>