<h1>Editing <%= resource.name %></h1>

<%=render :partial => 'form' %>

<%= link_to 'Show', admin_country_province_city_path(parent.country, parent, resource) %> |
<%= link_to 'Back', admin_country_province_cities_path(parent) %>