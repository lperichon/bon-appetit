<h1>Viewing <%= resource.name %></h1>

<p>
  <b>Name:</b>
  <%=h resource.name %>
</p>

<%= link_to 'Edit', edit_admin_country_division_city_path(parent.country, parent, resource) %> |
<%= link_to 'Back', admin_country_division_cities_path %>