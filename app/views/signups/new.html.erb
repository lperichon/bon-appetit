<h2>New user</h2>

<% semantic_form_for(@user, :url => signups_path) do |f| %>
  <% f.inputs do %>
    <%= render :partial => 'users/signup_fields', :locals => {:f => f} %>
  <% end %>
  <%= f.buttons %>
<% end %>

<%= link_to 'Back', root_path %>